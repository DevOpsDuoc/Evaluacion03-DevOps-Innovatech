apiVersion: v1
kind: Service
metadata:
  name: backend-ventas-service
  namespace: innovatech
spec:
  selector:
    app: backend-ventas-springboot
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP